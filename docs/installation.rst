============
Installation
============

At the command line::

    $ easy_install django-hdfs

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-hdfs
    $ pip install django-hdfs
